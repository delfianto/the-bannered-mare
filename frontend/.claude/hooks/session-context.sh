#!/usr/bin/env bash
# SessionStart: anchor the assistant to this project's real stack and counter
# unrelated skills present in the global skills directory.
printf '%s\n' '{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"The Bannered Mare frontend is a Vue 3.5 / TypeScript 6 (strict) SPA using Nuxt UI v4 (NOT Nuxt.js), Tailwind CSS v4, Pinia, Vue Router 5, openapi-fetch, and MSW. Build toolchain is Vite+ (the vp unified CLI from VoidZero) over Vite 8 / Rolldown / Oxc; Bun is the package manager, managed by vp. Primary commands: vp dev, vp build, vp check (fmt+lint+type), vp lint, vp fmt, vp install, vp update; bun run <script> still works as a wrapper. vp lives in ~/.vite-plus/bin — prepend that to PATH if a tool cannot find it. Unrelated global skills in ~/.claude (Rust m01-m15, rust-*, unsafe-checker, Python/backend helpers) do NOT apply here. QA gate before finishing: vp check && bun run build. Work on the main branch and commit freely; never git push unless the user explicitly asks."}}'
