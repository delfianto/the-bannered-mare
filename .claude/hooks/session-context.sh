#!/usr/bin/env bash
# SessionStart: anchor the assistant to this monorepo's real stacks (both halves)
# and counter unrelated global skills present in ~/.claude.
printf '%s\n' '{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"The Bannered Mare is a monorepo with two independent halves plus a docs site. backend/ is Python 3.14 / FastAPI / SQLAlchemy 2.0 / pgvector (QA gate before finishing: ruff format && ruff check --fix && basedpyright && pytest). frontend/ is a Vue 3.5 / TypeScript 6 (strict) SPA using Nuxt UI v4 (NOT Nuxt.js), Tailwind CSS v4, Pinia, Vue Router 5, openapi-fetch, and MSW; build toolchain is Vite+ (the vp unified CLI from VoidZero over Vite 8 / Rolldown / Oxc) and Bun is the package manager, managed by vp; vp lives in ~/.vite-plus/bin, prepend it to PATH if a tool cannot be found (QA gate: vp check && bun run build). docs/ is a VitePress documentation site (bun run docs:build). The shared API contract is openapi.json at the repo root. Unrelated global skills in ~/.claude (Rust m01-m15, rust-*, unsafe-checker) do NOT apply here. Work on the main branch and commit freely; never git push unless the user explicitly asks."}}'
