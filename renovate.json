{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "dependencyDashboard": false,
  "platformAutomerge": true,
  "labels": [
    "dependencies"
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Group and automerge all non-major updates",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "groupName": "non-major dependencies",
      "automerge": true
    },
    {
      "description": "Never propose major updates (avoid breaking changes)",
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": false
    },
    {
      "description": "vitest, its @vitest/* packages, and vite-plus (which vendors its own internal vitest) are tightly-coupled peer dependencies and must move together, or coverage-v8 vs vitest version mismatches break CI",
      "matchPackageNames": [
        "vitest",
        "@vitest/**",
        "vite-plus"
      ],
      "groupName": "vitest"
    }
  ]
}
